// components/Result.js
import React from 'react';

const Result = ({ propertyData, purchaseData, rehabData, loanData, incomeData, expensesData }) => {
  // Calculate ROI and generate the graph based on the data received
  // You can use a charting library like Chart.js or D3.js for graph generation

  return (
    <div>
      <h2>Result Page</h2>
      {/* Display the ROI graph and results here */}
      {/* You can use propertyData, purchaseData, rehabData, loanData, incomeData, and expensesData */}
    </div>
  );
};

export default Result;
